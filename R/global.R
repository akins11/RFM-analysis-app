# Usable objects ===============================================================
agg_label <- list(
  "min" = "Minimum",
  "mean" = "Average",
  "median" = "Median",
  "max" = "Maximum",
  "sum" = "Total"
)

plotly_modebar <- list(
  "zoom2d", "pan2d", "zoomIn2d", "autoScale2d",
  "resetScale2d", "hoverClosetCartesian",
  "hoverCompareCartesian", "sendDataToCloud",
  "toggleHover", "resetViews", "toggleSpikelines",
  "resetViewMapbox"
)

# Colors =======================================================================
spinner_color <- "#E066FF"

bar_color <- "#9600FF"
line_color <- "#9600FF"

box_sidebar_bg <- "#9932CC"

tbl_header_bg_color <- "#FAFAFF"
tbl_header_border_color <- "#171717"
tbl_border_color <- "#dfe2e5"
tbl_highlight_color <- "#f0f5f9"
tbl_ki_bg_color <- "#00FFF9"
tbl_second_ki_bg_color <- "#8DEEEE"
tbl_ki_bg_border_color <- "#48D1CC"
tbl_ki_text_color <- "#222222"

tbl_ki_bg_color_scale <- c("#7400B8", "#4EA8DE", "#80FFDB")
tbl_ki_bg_second_color_scale <- c("#6930C3", "#4A8FE7", "#59D2FE")

heatmap <- list(low = "#73FBD3", mid = "#59D2FE", high = "#5C7AFF")

treemap_color <- c("#00FFF9", "#9600FF", "#4900FF", "#EE1289", "#2F4F4F",
                   "#C1CDC1", "#FFA500", "#FF2400", "#FFDEAD", "#6B4226")

# Delete >>>
# from purple - cyan
pur_cy <- c("#7400B8", "#6930C3", "#5E60CE", "#5390D9", "#4EA8DE",
            "#48BFE3", "#56CFE1", "#64DFDF", "#72EFDD", "#80FFDB")

pal_pc <- c(rev(pur_cy), pur_cy)


# Text =========================================================================
tbl_header_text_size <- "15px"
tbl_ki_text_size <- "12px"

tbl_font_family <- "-apple-system, BlinkMacSystemFont, Segoe UI, Helvetica, Arial,
                    sans-serif"
